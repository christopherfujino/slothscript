### Name
files

### Program
# String -> File
# Here we do implicit casts: Process -> ProcessResult -> String
Process.new(["uname"]) -> File("foo")

# <- File => String
#let f = <- File("./README.md")
#print(f)

### Ast
((StmtDecl
  (ExprStmt
    (Binary
      (FuncInvoc
        (ObjDeref
          (IdRef
            Process
            [POS])
          new
          [POS])
        ((List
          ((String
            ((FullString
              uname
              [POS]))
            [POS]))
          [POS]))
        [POS])
      (FuncInvoc
        (IdRef
          File
          [POS])
        ((String
          ((FullString
            foo
            [POS]))
          [POS]))
        [POS])
      RightArrow
      [POS]))))

### Stdout


### Failure


