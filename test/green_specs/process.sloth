### Name
process

### Program
#let result = Process.new(["uname"]) | Process.new(["tr", "x", "s"])!
Process.new(["uname", "-a"])!

### Ast
((StmtDecl
  (ExprStmt
    (UnaryExpr
      (target
        (FuncInvoc
          (ObjDeref
            (IdRef
              Process
              [POS])
            new
            [POS])
          ((List
            ((String
              ((FullString
                uname
                [POS]))
              [POS])
              (String
                ((FullString
                  -a
                  [POS]))
                [POS]))
            [POS]))
          [POS]))
      (pos
        [POS])
      (is_prefix
        false)
      (operator
        Bang)))))

### Stdout


### Failure


