### Name
first class conditionals

### Program
let b = false;
let v = if b {
  1;
} else {
  2;
};
print(v);

if (let x = false) {
  # Unreachable
  assert(false)
}

# x is bound here; is this right?
assert(x == false)

### Ast
((StmtDecl
  (ExprStmt
    (LetExpr
      b
      (Bool
        false
        [POS])
      [POS])))
  (StmtDecl
    (ExprStmt
      (LetExpr
        v
        (IfExpr
          (IfCont
            (conditional
              (IdRef
                b
                [POS]))
            (block
              ((ExprStmt
                (Num
                  1
                  [POS]))))
            (continuation
              ((ElseCont
                ((ExprStmt
                  (Num
                    2
                    [POS])))
                [POS])))
            (pos
              [POS]))
          [POS])
        [POS])))
  (StmtDecl
    (ExprStmt
      (FuncInvoc
        (IdRef
          print
          [POS])
        ((IdRef
          v
          [POS]))
        [POS])))
  (StmtDecl
    (ExprStmt
      (IfExpr
        (IfCont
          (conditional
            (LetExpr
              x
              (Bool
                false
                [POS])
              [POS]))
          (block
            ((ExprStmt
              (FuncInvoc
                (IdRef
                  assert
                  [POS])
                ((Bool
                  false
                  [POS]))
                [POS]))))
          (continuation
            ())
          (pos
            [POS]))
        [POS])))
  (StmtDecl
    (ExprStmt
      (FuncInvoc
        (IdRef
          assert
          [POS])
        ((Equality
          (IdRef
            x
            [POS])
          (Bool
            false
            [POS])
          true
          [POS]))
        [POS]))))

### Stdout
2

### Failure

### Processes
()
