### Name
return, break, continue, error

### Program
func earlyReturn() {
  return
  assert(false)
}

earlyReturn()

### Ast
((FuncDecl
  (name
    earlyReturn)
  (parameters
    ())
  (block
    ((BreakingStmt
      Return
      ()
      [POS])
      (ExprStmt
        (FuncInvoc
          (IdRef
            assert
            [POS])
          ((Bool
            false
            [POS]))
          [POS]))))
  (pos
    [POS]))
  (StmtDecl
    (ExprStmt
      (FuncInvoc
        (IdRef
          earlyReturn
          [POS])
        ()
        [POS]))))

### Stdout


### Failure


