### Name
first class func

### Program
func makeCounter() {
  let x = 0
  func() {
    x = x + 1
    x
  }
}

let counter = makeCounter()
print(counter())
print(counter())

### Ast
((FuncDecl
  (name
    makeCounter)
  (parameters
    ())
  (block
    ((LetStmt
      x
      (Num
        0))
      (ExprStmt
        (FuncExpr
          (parameters
            ())
          (block
            ((AssignStmt
              x
              (MethodInvoc
                (receiver
                  (IdRef
                    x))
                (target
                  +)
                (args
                  ((Num
                    1)))))
              (ExprStmt
                (IdRef
                  x)))))))))
  (StmtDecl
    (LetStmt
      counter
      (FuncInvoc
        (IdRef
          makeCounter)
        ())))
  (StmtDecl
    (ExprStmt
      (FuncInvoc
        (IdRef
          print)
        ((FuncInvoc
          (IdRef
            counter)
          ())))))
  (StmtDecl
    (ExprStmt
      (FuncInvoc
        (IdRef
          print)
        ((FuncInvoc
          (IdRef
            counter)
          ()))))))

### Stdout
1
2

### Failure


