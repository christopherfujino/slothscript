## Binary

### Go

From high to low precedence (left-associative):

`*`, `/`, `%`, `<<`, `>>`, `&`, `&^`
`+`, `-`, `|`, `^`
`==`, `!=`, `<`, `<=`, `>`, `>=`
`&&`
`||`

### C

`*`, `/`, `%`
`+`, `-`
`<`, `<=`, `>`, `>=`
`==`, `!=`
`&&`
`||`

### Sloth

`*`, `/`, `%`
`+`, `-`, `|`
`==`, `!=`, `<`, `<=`, `>`, `>=`
`and`
`or`
